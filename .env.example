# ── Contact form (Resend) ─────────────────────────────────────────────
# Create a key at https://resend.com/api-keys
RESEND_API_KEY=

# Where contact submissions are delivered.
# On Resend's free tier WITHOUT a verified domain, this MUST be the email
# you signed up to Resend with. Defaults to the address in data/content.ts.
CONTACT_TO_EMAIL=akanaiyalalp@mun.ca

# Sender address. Leave as-is to use Resend's shared sender (no domain setup).
# Once you verify your own domain in Resend, change this to e.g.
# "Aksh Patel <hello@yourdomain.com>".
CONTACT_FROM_EMAIL=Portfolio <onboarding@resend.dev>

# ── SEO ───────────────────────────────────────────────────────────────
# Your real production URL (no trailing slash). Used for sitemap, canonical
# tags and Open Graph. Set this in Vercel to your live domain.
NEXT_PUBLIC_SITE_URL=https://www.axsh.me

# Google Search Console verification token (HTML-tag method).
# Paste only the content value of the meta tag Google gives you.
GOOGLE_SITE_VERIFICATION=

# ── Admin dashboard (SERVER-ONLY SECRETS) ─────────────────────────────
# These MUST NOT use the NEXT_PUBLIC_ prefix — they must never reach the
# browser. Set them in Vercel → Settings → Environment Variables.
#
# The password you type at /admin/login:
ADMIN_PASSWORD=
# A long random string used to sign session cookies (32+ chars).
# Generate one with:  openssl rand -base64 32
SESSION_SECRET=

# ── Database (Supabase) ───────────────────────────────────────────────
# Create a free project at https://supabase.com, run the SQL to make the
# `site_content` table, then paste your Project URL + service-role key here.
# The service-role key is a SECRET (server-only) — never NEXT_PUBLIC_.
# Without these, the public site still works (fallback content) but the
# admin can't save.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
